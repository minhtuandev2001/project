const app = (function () {
  // private
  const cars = [];
  // muốn dùng thì phải return ra
  return {
    add(car) {
      cars[index] = car;
    },
    edit(car) {
      cars[index] = car;
    },
    delete(index) {
      cars.splice(index, 1);
    },
  };
})();
